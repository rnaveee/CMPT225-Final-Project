# Final Project - CMPT225

Project by: Dhared Drew, Ryan Nave

priority_queue_benchmark.cpp contains the main c++ code to test the 3 heaps.
It outputs benchmark_data.csv for the MATLAB code to process and analyze, along with plot the data

data_visualization.m is the MATLAB script to analyze and plot all of the data from benchmark_data.csv
it produces a summary_table.csv file, which is just a simplified version of the benchmark_data.csv file

